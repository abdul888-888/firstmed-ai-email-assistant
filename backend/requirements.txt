# Runtime dependencies — FirstMed backend (Phase 1)
# Target: Python 3.13 (also runs on 3.11+)

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
SQLAlchemy==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver (runtime/prod)
alembic==1.14.0
pgvector==0.5.0          # Vector column type + pgvector extension bindings

# --- Security / auth ---
bcrypt==4.2.1
PyJWT==2.10.1
cryptography==44.0.0     # Fernet encryption for stored OAuth tokens

# --- Cache / broker / jobs ---
redis==5.2.1
celery==5.4.0

# --- Logging / observability ---
structlog==24.4.0

# --- HTTP client (integrations, later phases) ---
httpx==0.28.1

# --- AI providers (Phase 5) ---
anthropic==0.116.0

# --- Embeddings / semantic retrieval (Phase 9) ---
# Local, offline embeddings (ONNX). Downloads the model on first use.
# Swap to OpenAI/Voyage via EMBEDDING_PROVIDER (add their SDK if used).
fastembed==0.4.2
