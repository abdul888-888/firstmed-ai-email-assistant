{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "services": {
      "api": {
        "startCommand": "sh -c 'cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
      },
      "worker": {
        "startCommand": "cd backend && celery -A app.workers.celery_app worker --loglevel=info --concurrency=4"
      },
      "beat": {
        "startCommand": "cd backend && celery -A app.workers.celery_app beat --loglevel=info"
      }
    }
  }
}
